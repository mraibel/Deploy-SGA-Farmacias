apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: msvc-gateway-ingress
spec:
  rules:
    - host: api-gateway.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: msvc-gateway
                port:
                  number: 80